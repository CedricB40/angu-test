import { StatutAffichagePipe } from './statut-affichage-pipe';

describe('StatutAffichagePipe', () => {
  it('create an instance', () => {
    const pipe = new StatutAffichagePipe();
    expect(pipe).toBeTruthy();
  });
});
